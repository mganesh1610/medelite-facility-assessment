{
  "name": "medelite-facility-assessment",
  "private": true,
  "scripts": {
    "frontend:dev": "pnpm --dir frontend dev",
    "frontend:build": "pnpm --dir frontend build",
    "frontend:preview": "pnpm --dir frontend preview",
    "backend:dev": "python -m uvicorn app.main:app --reload --app-dir backend --port 8000",
    "test:backend": "python -m pytest backend/tests",
    "test": "pnpm frontend:build && python -m pytest backend/tests"
  }
}

